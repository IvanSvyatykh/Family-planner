<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FamilyPage.FamilyView"
             Title="Family"
             Shell.FlyoutBehavior="Locked"
             >
    <RefreshView>
        <ScrollView>
            <Grid>
                <VerticalStackLayout IsVisible="{Binding IsExistFamily}">
                    <Label Text="123"/>
                </VerticalStackLayout>
                <VerticalStackLayout IsVisible="{Binding FamilyExist}">
                    <Label Text="143"/>
                </VerticalStackLayout>
            </Grid>
        </ScrollView>
       
    </RefreshView>
    
    


</ContentPage>